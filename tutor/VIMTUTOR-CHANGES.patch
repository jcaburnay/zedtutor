diff --git a/tmp/vimtutor1 b/tutor/tutor.txt
index 790de39..a72bba9 100644
--- a/tmp/vimtutor1
+++ b/tutor/tutor.txt
@@ -1,26 +1,33 @@
+
 ===============================================================================
-=    W e l c o m e   t o   t h e   V I M   T u t o r    -    Version 1.7      =
+=                    Z E D   V I M   T U T O R                                =
 ===============================================================================
-=			       C H A P T E R   ONE			      =
+=                              CHAPTER ONE                                     =
 ===============================================================================
 
-     Vim is a very powerful editor that has many commands, too many to
-     explain in a tutor such as this.  This tutor is designed to describe
-     enough of the commands that you will be able to easily use Vim as
-     an all-purpose editor.
-     The approximate time required to complete the tutor is 30 minutes,
-     depending upon how much time is spent with experimentation.
+     Zed includes a Vim mode that brings Vim's modal editing model into Zed.
+     This tutor teaches the core motions and editing commands by having you
+     use them directly inside this file.
+
+     The approximate time required to complete this chapter is 30-45 minutes,
+     depending on how much time you spend experimenting.
 
      ATTENTION:
-     The commands in the lessons will modify the text.  Make a copy of this
-     file to practice on (if you started "vimtutor" this is already a copy).
+     The commands in the lessons will modify this text.  That is expected.
+     When you run `zedtutor`, a fresh practice copy is created, so you can
+     experiment freely without changing the pristine tutor shipped with the
+     project.
+
+     This tutor is designed to teach by use.  Execute the commands in each
+     lesson instead of trying to memorize everything at once.
+
+     Before starting:
+       1. Make sure Zed Vim mode is enabled.
+       2. Press <ESC> so you are in Normal mode.
+       3. Press j enough times to move down to Lesson 1.1.1.
 
-     It is important to remember that this tutor is set up to teach by
-     use.  That means that you need to execute the commands to learn them
-     properly.  If you only read the text, you will forget the commands!
-     Now, make sure that your Caps-Lock key is NOT depressed and press
-     the   j   key enough times to move the cursor so that lesson 1.1.1
-     completely fills the screen.
+     This tutorial is adapted from the Vim Tutor distributed with Vim.
+     See the repository's attribution and Vim License files for details.
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 			Lesson 1.1.1:  MOVING THE CURSOR
 
@@ -43,30 +50,30 @@ NOTE: If you are ever unsure about something you typed, press <ESC> to place
 
 NOTE: The cursor keys should also work.  But using hjkl you will be able to
       move around much faster, once you get used to it.  Really!
-
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
-			    Lesson 1.1.2: EXITING VIM
+			    Lesson 1.1.2: CLOSING THE TUTOR
 
 
   !! NOTE: Before executing any of the steps below, read this entire lesson!!
 
-  1. Press the <ESC> key (to make sure you are in Normal mode).
+  1. Press the <ESC> key to make sure you are in Normal mode.
 
-  2. Type:	:q! <ENTER>.
-     This exits the editor, DISCARDING any changes you have made.
+  2. Type:	:q! <ENTER>
 
-  3. Get back here by executing the command that got you into this tutor. That
-     might be:  vimtutor <ENTER>
+     Zed closes the current practice buffer without saving your changes.
 
-  4. If you have these steps memorized and are confident, execute steps
-     1 through 3 to exit and re-enter the editor.
+  3. From your terminal, run:
 
-NOTE:  :q! <ENTER>  discards any changes you made.  In a few lessons you
-       will learn how to save the changes to a file.
+	zedtutor
 
-  5. Move the cursor down to lesson 1.1.3.
+     A fresh copy of the tutorial will open in a dedicated Zed window.
+
+  4. You may try this now, or simply continue to the next lesson.
 
+NOTE: Because zedtutor creates a disposable practice copy, you can restart the
+      tutor whenever you want a clean version.
 
+  5. Move the cursor down to lesson 1.1.3.
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 		     Lesson 1.1.3: TEXT EDITING - DELETION
 
@@ -87,9 +94,6 @@ NOTE:  :q! <ENTER>  discards any changes you made.  In a few lessons you
   5. Now that the line is correct, go on to lesson 1.1.4.
 
 NOTE: As you go through this tutor, do not try to memorize, learn by usage.
-
-
-
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 		      Lesson 1.1.4: TEXT EDITING - INSERTION
 
@@ -110,9 +114,6 @@ NOTE: As you go through this tutor, do not try to memorize, learn by usage.
 ---> There is some text missing from this line.
 
   5. When you are comfortable inserting text move to lesson 1.1.5.
-
-
-
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 		     Lesson 1.1.5: TEXT EDITING - APPENDING
 
@@ -135,76 +136,70 @@ NOTE: As you go through this tutor, do not try to memorize, learn by usage.
      There is also some text missing here.
 
   5. When you are comfortable appending text move to lesson 1.1.6.
-
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
-		     Lesson 1.1.6: EDITING A FILE
+		     Lesson 1.1.6: SAVING YOUR SESSION
 
-		    ** Use  :wq  to save a file and exit. **
+		    ** Use  :w  to save the current file. **
 
-  !! NOTE: Before executing any of the steps below, read this entire lesson!!
+  1. Press <ESC> to make sure you are in Normal mode.
+
+  2. Type:	:w <ENTER>
 
-  1.  If you have access to another terminal, do the following there.
-      Otherwise, exit this tutor as you did in lesson 1.1.2:  :q!
+     Zed saves the current practice file.
 
-  2. At the shell prompt type this command:  vim file.txt <ENTER>
-     'vim' is the command to start the Vim editor, 'file.txt' is the name of
-     the file you wish to edit.  Use the name of a file that you can change.
+  3. Continue editing after saving.  Saving this practice copy does not modify
+     the pristine tutor shipped with zedtutor.
 
-  3. Insert and delete text as you learned in the previous lessons.
+  4. To save and close the current buffer, type:
 
-  4. Save the file with changes and exit Vim with:  :wq <ENTER>
+	:wq <ENTER>
 
-  5. If you have quit vimtutor in step 1 restart the vimtutor and move down to
-     the following summary.
+  5. To close without saving your latest changes, type:
 
-  6. After reading the above steps and understanding them: do it.
+	:q! <ENTER>
 
+NOTE: Running `zedtutor` again always creates a fresh practice session.
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 			       Lesson 1.1 SUMMARY
 
 
-  1. The cursor is moved using either the arrow keys or the hjkl keys.
+  1. Move the cursor in Normal mode with:
+
 	 h (left)	j (down)       k (up)	    l (right)
 
-  2. To start Vim from the shell prompt type:  vim FILENAME <ENTER>
+  2. Press <ESC> to return to Normal mode or cancel a partially typed command.
 
-  3. To exit Vim type:	   <ESC>   :q!	 <ENTER>  to trash all changes.
-	     OR type:	   <ESC>   :wq	 <ENTER>  to save the changes.
+  3. To save the current practice buffer type:        :w <ENTER>
+     To save and close the current buffer type:        :wq <ENTER>
+     To close without saving the latest changes type:  :q! <ENTER>
 
   4. To delete the character at the cursor type:  x
 
   5. To insert or append text type:
 	 i   type inserted text   <ESC>		insert before the cursor
-	 A   type appended text   <ESC>         append after the line
-
-NOTE: Pressing <ESC> will place you in Normal mode or will cancel
-      an unwanted and partially completed command.
+	 A   type appended text   <ESC>		append at the end of the line
 
 Now continue with lesson 1.2.
-
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 			Lesson 1.2.1: DELETION COMMANDS
 
 
 		       ** Type  dw  to delete a word. **
 
-  1. Press  <ESC>  to make sure you are in Normal mode.
+  1. Press <ESC> to make sure you are in Normal mode.
 
   2. Move the cursor to the line below marked --->.
 
   3. Move the cursor to the beginning of a word that needs to be deleted.
 
-  4. Type   dw	 to make the word disappear.
-
-  NOTE: The letter  d  will appear on the last line of the screen as you type
-	it.  Vim is waiting for you to type  w .  If you see another character
-	than  d  you typed something wrong; press  <ESC>  and start over.
+  4. Type   dw   to make the word disappear.
 
 ---> There are a some words fun that don't belong paper in this sentence.
 
   5. Repeat steps 3 and 4 until the sentence is correct and go to lesson 1.2.2.
 
-
+NOTE: If you start an unwanted command, press <ESC> to return to Normal mode
+      and try again.
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 		      Lesson 1.2.2: MORE DELETION COMMANDS
 
@@ -223,11 +218,6 @@ Now continue with lesson 1.2.
 
 
   5. Move on to lesson 1.2.3 to understand what is happening.
-
-
-
-
-
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 		     Lesson 1.2.3: ON OPERATORS AND MOTIONS
 
@@ -250,7 +240,6 @@ Now continue with lesson 1.2.
 
 NOTE:  Pressing just the motion while in Normal mode without an operator will
        move the cursor as specified.
-
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 		     Lesson 1.2.4: USING A COUNT FOR A MOTION
 
@@ -270,10 +259,6 @@ NOTE:  Pressing just the motion while in Normal mode without an operator will
 ---> This is just a line with words you can move around in.
 
   6. Move on to lesson 1.2.5.
-
-
-
-
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 		     Lesson 1.2.5: USING A COUNT TO DELETE MORE
 
@@ -292,11 +277,6 @@ NOTE:  Pressing just the motion while in Normal mode without an operator will
      UPPER CASE words with one command.
 
 --->  this ABC DE line FGHI JK LMN OP of words is Q RS TUV cleaned up.
-
-
-
-
-
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 			 Lesson 1.2.6: OPERATING ON LINES
 
@@ -324,25 +304,24 @@ Doubling to operate on a line also works for operators mentioned below.
 			 Lesson 1.2.7: THE UNDO COMMAND
 
 
-   ** Press  u	to undo the last commands,   U  to fix a whole line. **
+       ** Press  u  to undo and  CTRL-R  to redo. **
 
   1. Move the cursor to the line below marked ---> and place it on the
      first error.
+
   2. Type  x  to delete the first unwanted character.
-  3. Now type  u  to undo the last command executed.
-  4. This time fix all the errors on the line using the  x  command.
-  5. Now type a capital  U  to return the line to its original state.
-  6. Now type  u  a few times to undo the  U  and preceding commands.
-  7. Now type CTRL-R (keeping CTRL key pressed while hitting R) a few times
-     to redo the commands (undo the undos).
 
----> Fiix the errors oon thhis line and reeplace them witth undo.
+  3. Type  u  to undo that deletion.
 
-  8. These are very useful commands.  Now move on to the lesson 1.2 Summary.
+  4. Fix several errors on the line using  x .
 
+  5. Type  u  a few times to undo those changes one by one.
 
+  6. Type CTRL-R (hold Control while pressing R) a few times to redo them.
 
+---> Fiix the errors oon thhis line and reeplace them witth undo.
 
+  7. These are very useful commands.  Now move on to the Lesson 1.2 Summary.
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 			       Lesson 1.2 SUMMARY
 
@@ -351,21 +330,21 @@ Doubling to operate on a line also works for operators mentioned below.
   3. To delete from the cursor to the end of a line type:       d$
   4. To delete a whole line type:                               dd
 
-  5. To repeat a motion prepend it with a number:   2w
-  6. The format for a change command is:
-               operator   [number]   motion
-     where:
-       operator - is what to do, such as  d  for delete
-       [number] - is an optional count to repeat the motion
-       motion   - moves over the text to operate on, such as  w (word),
-		  e (end of word),  $ (end of the line), etc.
+  5. To repeat a motion, prepend it with a number:   2w
 
-  7. To move to the start of the line use a zero:  0
+  6. The general operator form is:
+
+	       operator   [number]   motion
+
+     where:
+       operator - is what to do, such as d for delete
+       [number] - is an optional count
+       motion   - describes what text to operate on, such as w, e, or $
 
-  8. To undo previous actions, type:           u  (lowercase u)
-     To undo all the changes on a line, type:  U  (capital U)
-     To undo the undos, type:                  CTRL-R
+  7. To move to the start of the line use:          0
 
+  8. To undo previous actions type:                  u
+     To redo undone actions type:                    CTRL-R
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 			 Lesson 1.3.1: THE PUT COMMAND
 
@@ -374,7 +353,7 @@ Doubling to operate on a line also works for operators mentioned below.
 
   1. Move the cursor to the first line below marked --->.
 
-  2. Type  dd  to delete the line and store it in a Vim register.
+  2. Type  dd  to delete the line and store it in a register.
 
   3. Move the cursor to the c) line, ABOVE where the deleted line should go.
 
@@ -386,9 +365,6 @@ Doubling to operate on a line also works for operators mentioned below.
 ---> b) Violets are blue,
 ---> c) Intelligence is learned,
 ---> a) Roses are red,
-
-
-
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 		       Lesson 1.3.2: THE REPLACE COMMAND
 
@@ -409,9 +385,6 @@ Doubling to operate on a line also works for operators mentioned below.
   5. Now move on to lesson 1.3.3.
 
 NOTE: Remember that you should be learning by doing, not memorization.
-
-
-
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 			Lesson 1.3.3: THE CHANGE OPERATOR
 
@@ -433,8 +406,6 @@ NOTE: Remember that you should be learning by doing, not memorization.
 
 Notice that  ce  deletes the word and places you in Insert mode.
              cc  does the same for the whole line.
-
-
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 		       Lesson 1.3.4: MORE CHANGES USING c
 
@@ -457,7 +428,6 @@ Notice that  ce  deletes the word and places you in Insert mode.
 ---> The end of this line needs to be corrected using the  c$  command.
 
 NOTE:  You can use the Backspace key to correct mistakes while typing.
-
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 			       Lesson 1.3 SUMMARY
 
@@ -478,78 +448,73 @@ NOTE:  You can use the Backspace key to correct mistakes while typing.
 	 c   [number]   motion
 
 Now go on to the next lesson.
+~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
+		  Lesson 1.4.1: MOVING THROUGH THE FILE
 
 
+       ** Type  G  to move to the end and  gg  to move to the start. **
 
-~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
-		  Lesson 1.4.1: CURSOR LOCATION AND FILE STATUS
+  1. Press  G  to move to the bottom of this tutor.
 
-  ** Type CTRL-G to show your location in the file and the file status.
-     Type  G  to move to a line in the file. **
+  2. Type  gg  to move back to the beginning.
 
-  NOTE: Read this entire lesson before executing any of the steps!!
+  3. Type a line number followed by  G  to jump directly to that line.
+     For example:
 
-  1. Hold down the Ctrl key and press  g .  We call this CTRL-G.
-     A message will appear at the bottom of the page with the filename and the
-     position in the file.  Remember the line number for Step 3.
+	200G
 
-NOTE:  You may see the cursor position in the lower right corner of the screen
-       This happens when the 'ruler' option is set (see  :help 'ruler'  )
+     moves to line 200.
 
-  2. Press  G  to move you to the bottom of the file.
-     Type  gg  to move you to the start of the file.
+  4. Zed also accepts a line number from the  :  command palette.  For example:
 
-  3. Type the number of the line you were on and then  G .  This will
-     return you to the line you were on when you first pressed CTRL-G.
+	:200 <ENTER>
 
-  4. If you feel confident to do this, execute steps 1 through 3.
+  5. Try jumping to a few different lines, then return to this lesson.
 
+NOTE: Zed normally shows line numbers in the gutter.  Later you will also
+      practice the :set number and :set relativenumber commands.
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 			Lesson 1.4.2: THE SEARCH COMMAND
 
 
-     ** Type  /  followed by a phrase to search for the phrase. **
+     ** Type  /  followed by a phrase to search forward. **
 
-  1. In Normal mode type the  /  character.  Notice that it and the cursor
-     appear at the bottom of the screen as with the  :	command.
+  1. In Normal mode type the  /  character.
 
-  2. Now type 'errroor' <ENTER>.  This is the word you want to search for.
+  2. Type  errroor  and press <ENTER>.
 
-  3. To search for the same phrase again, simply type  n .
-     To search for the same phrase in the opposite direction, type  N .
+  3. To search for the same phrase again, type  n .
+     To search for it in the opposite direction, type  N .
 
-  4. To search for a phrase in the backward direction, use  ?  instead of  / .
+  4. To search backward for a phrase, use  ?  instead of  / .
 
-  5. To go back to where you came from press  CTRL-O  (Keep Ctrl down while
-     pressing the letter o).  Repeat to go back further.  CTRL-I goes forward.
+  5. CTRL-O goes back to older jump locations and CTRL-I goes forward through
+     newer jump locations.  Try them after moving between a few search matches.
 
 --->  "errroor" is not the way to spell error;  errroor is an error.
-NOTE: When the search reaches the end of the file it will continue at the
-      start, unless the 'wrapscan' option has been reset.
 
+NOTE: Zed uses its own search system underneath Vim mode.  The familiar
+      /, ?, n, and N commands still provide the Vim-style workflow.
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
-		   Lesson 1.4.3: MATCHING PARENTHESES SEARCH
+		   Lesson 1.4.3: MATCHING BRACKETS
 
 
-	      ** Type  %  to find a matching ),], or } . **
+	      ** Type  %  to jump to a matching bracket. **
 
   1. Place the cursor on any (, [, or { in the line below marked --->.
 
-  2. Now type the  %  character.
+  2. Type  % .
 
-  3. The cursor will move to the matching parenthesis or bracket.
+  3. The cursor moves to the corresponding matching delimiter.
 
-  4. Type  %  to move the cursor to the other matching bracket.
+  4. Type  %  again to move back.
 
-  5. Move the cursor to another (,),[,],{ or } and see what  %  does.
+  5. Move to another delimiter and try it again.
 
 ---> This ( is a test line with ('s, ['s ] and {'s } in it. ))
 
-
-NOTE: This is very useful in debugging a program with unmatched parentheses!
-
-
-
+NOTE: Zed's Vim mode uses semantic parsing where it makes sense, so % can be
+      language-aware when you use it in source-code files.
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 		      Lesson 1.4.4: THE SUBSTITUTE COMMAND
 
@@ -558,159 +523,176 @@ NOTE: This is very useful in debugging a program with unmatched parentheses!
 
   1. Move the cursor to the line below marked --->.
 
-  2. Type  :s/thee/the <ENTER>  .  Note that this command only changes the
-     first occurrence of "thee" in the line.
+  2. Type:
+
+	:s/thee/the <ENTER>
+
+     This changes the first occurrence of "thee" on the current line.
+
+  3. Now type:
+
+	:s/thee/the/g <ENTER>
 
-  3. Now type  :s/thee/the/g .  Adding the  g  flag means to substitute
-     globally in the line, change all occurrences of "thee" in the line.
+     The g flag changes every occurrence on the current line.
 
 ---> thee best time to see thee flowers is in thee spring.
 
-  4. To change every occurrence of a character string between two lines,
-     type   :#,#s/old/new/g    where #,# are the line numbers of the range
-                               of lines where the substitution is to be done.
-     Type   :%s/old/new/g      to change every occurrence in the whole file.
-     Type   :%s/old/new/gc     to find every occurrence in the whole file,
-     			       with a prompt whether to substitute or not.
+  4. To replace text across a range of lines, use:
 
+	:#,#s/old/new/g
+
+     where #,# are the first and last line numbers.
+
+  5. To replace throughout the entire file, use:
+
+	:%s/old/new/g
+
+NOTE: Zed implements Vim-style substitution using Zed's regular-expression
+      engine.  Most simple substitutions look the same, but advanced regex
+      syntax differs from Vim.  In particular, Zed uses normal capture groups
+      like (text) and replacement groups such as $1.
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 			       Lesson 1.4 SUMMARY
 
 
-  1. CTRL-G  displays your location in the file and the file status.
-             G  moves to the end of the file.
-     number  G  moves to that line number.
-            gg  moves to the first line.
+  1. G moves to the end of the file.
+     gg moves to the beginning.
+     number G moves to that line number.
+     :number also jumps to a line in Zed.
 
-  2. Typing  /	followed by a phrase searches FORWARD for the phrase.
-     Typing  ?	followed by a phrase searches BACKWARD for the phrase.
-     After a search type  n  to find the next occurrence in the same direction
-     or  N  to search in the opposite direction.
-     CTRL-O takes you back to older positions, CTRL-I to newer positions.
+  2. /phrase searches forward.
+     ?phrase searches backward.
+     n repeats a search in the same direction.
+     N repeats a search in the opposite direction.
+     CTRL-O moves to an older jump; CTRL-I moves to a newer jump.
 
-  3. Typing  %	while the cursor is on a (,),[,],{, or } goes to its match.
+  3. % jumps between matching delimiters and may use Zed's semantic parsing.
 
-  4. To substitute new for the first old in a line type    :s/old/new
-     To substitute new for all 'old's on a line type	   :s/old/new/g
-     To substitute phrases between two line #'s type	   :#,#s/old/new/g
-     To substitute all occurrences in the file type	   :%s/old/new/g
-     To ask for confirmation each time add 'c'		   :%s/old/new/gc
+  4. Substitute commands include:
 
+	:s/old/new		replace the first match on the current line
+	:s/old/new/g		replace all matches on the current line
+	:#,#s/old/new/g		replace within a line range
+	:%s/old/new/g		replace throughout the file
+
+     Zed's advanced regular-expression syntax differs slightly from Vim's.
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
-		Lesson 1.5.1: HOW TO EXECUTE AN EXTERNAL COMMAND
+		Lesson 1.5.1: VISUAL MODE
+
+
+	 ** Use  v  to select text before operating on it. **
 
+  1. Move the cursor to the word SELECT in the line below.
 
-   ** Type  :!	followed by an external command to execute that command. **
+  2. Press  v  to enter character-wise Visual mode.
 
-  1. Type the familiar command	:  to set the cursor at the bottom of the
-     screen.  This allows you to enter a command-line command.
+  3. Move the cursor until the unwanted phrase is highlighted.
 
-  2. Now type the  !  (exclamation point) character.  This allows you to
-     execute any external shell command.
+  4. Press  d  to delete the selection.
 
-  3. As an example type   ls   following the ! and then hit <ENTER>.  This
-     will show you a listing of your directory, just as if you were at the
-     shell prompt.  Or use  :!dir  if ls doesn't work.
+---> Keep this. SELECT AND DELETE THIS. Keep this too.
 
-NOTE:  It is possible to execute any external command this way, also with
-       arguments.
+  5. Press  u  to undo the deletion.
 
-NOTE:  All  :  commands must be finished by hitting <ENTER>
-       From here on we will not always mention it.
+  6. Try  V  to enter line-wise Visual mode on one of the lines below.
 
+---> line one
+---> line two
+---> line three
 
+NOTE: Visual mode lets you choose the text first, then apply an operator such
+      as d, c, or y.
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
-		      Lesson 1.5.2: MORE ON WRITING FILES
+		      Lesson 1.5.2: SPLITTING PANES
 
 
-     ** To save the changes made to the text, type  :w FILENAME  **
+       ** Use  :vs  and  :sp  to split the current Zed pane. **
 
-  1. Type  :!dir  or  :!ls  to get a listing of your directory.
-     You already know you must hit <ENTER> after this.
+  1. Press <ESC> to make sure you are in Normal mode.
 
-  2. Choose a filename that does not exist yet, such as TEST.
+  2. Type:
 
-  3. Now type:	 :w TEST   (where TEST is the filename you chose.)
+	:vs <ENTER>
 
-  4. This saves the whole file (the Vim Tutor) under the name TEST.
-     To verify this, type    :!dir  or  :!ls   again to see your directory.
+     Zed splits the current pane vertically.
 
-NOTE: If you were to exit Vim and start it again with  vim TEST , the file
-      would be an exact copy of the tutor when you saved it.
+  3. Type:
 
-  5. Now remove the file by typing (Windows):   :!del TEST
-				or (Unix):	:!rm TEST
+	:sp <ENTER>
 
+     Zed splits the current pane horizontally.
 
+  4. Type:
+
+	:q <ENTER>
+
+     to close the current buffer in the active pane.
+
+NOTE: Zed supports Vim-style pane-management commands, but its command line is
+      not a complete Vim Ex implementation.
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
-		    Lesson 1.5.3: SELECTING TEXT TO WRITE
+		    Lesson 1.5.3: BUFFERS AND THE PROJECT PANEL
 
 
-	** To save part of the file, type  v  motion  :w FILENAME **
+	 ** Use  :ls  to view buffers and  :Explore  to open the project panel. **
 
-  1. Move the cursor to this line.
+  1. Type:
 
-  2. Press  v  and move the cursor to the fifth item below.  Notice that the
-     text is highlighted.
+	:ls <ENTER>
 
-  3. Press the  :  character.  At the bottom of the screen  :'<,'> will appear.
+     Zed shows the currently open buffers.
 
-  4. Type  w TEST  , where TEST is a filename that does not exist yet.  Verify
-     that you see  :'<,'>w TEST  before you press <ENTER>.
+  2. Type:
 
-  5. Vim will write the selected lines to the file TEST.  Use  :!dir  or  :!ls
-     to see it.  Do not remove it yet!  We will use it in the next lesson.
+	:Explore <ENTER>
 
-NOTE:  Pressing  v  starts Visual selection.  You can move the cursor around
-       to make the selection bigger or smaller.  Then you can use an operator
-       to do something with the text.  For example,  d  deletes the text.
+     Zed opens the project panel.
 
-~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
-		   Lesson 1.5.4: RETRIEVING AND MERGING FILES
+  3. In the project panel, Vim-style navigation is available.  You can use
+     h, j, k, and l to move around, and o to open an item.
+
+  4. Return to this tutor when you are finished exploring.
 
+NOTE: This is an example of Zed keeping Vim muscle memory while integrating
+      with Zed's own interface.
+~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
+		   Lesson 1.5.4: OPENING THE TERMINAL
 
-       ** To insert the contents of a file, type  :r FILENAME  **
 
-  1. Place the cursor just above this line.
+	      ** Type  :term  to open Zed's terminal. **
 
-NOTE:  After executing Step 2 you will see text from lesson 1.5.3.  Then move
-       DOWN to see this lesson again.
+  1. Press <ESC> to make sure you are in Normal mode.
 
-  2. Now retrieve your TEST file using the command   :r TEST   where TEST is
-     the name of the file you used.
-     The file you retrieve is placed below the cursor line.
+  2. Type:
 
-  3. To verify that a file was retrieved, cursor back and notice that there
-     are now two copies of lesson 1.5.3, the original and the file version.
+	:term <ENTER>
 
-NOTE:  You can also read the output of an external command.  For example,
-       :r !ls  reads the output of the ls command and puts it below the
-       cursor.
+     Zed opens its terminal panel.
 
+  3. Use the terminal normally, then return to this tutor.
 
+NOTE: Classic Vim's :!command, :r !command, and filename arguments to commands
+      are not the focus here.  Zed exposes its own panels and workflows instead.
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 			       Lesson 1.5 SUMMARY
 
 
-  1.  :!command  executes an external command.
+  1. v starts character-wise Visual mode.
+     V starts line-wise Visual mode.
 
-      Some useful examples are:
-	 (Windows)	  (Unix)
-	  :!dir		   :!ls		   -  shows a directory listing.
-	  :!del FILENAME   :!rm FILENAME   -  removes file FILENAME.
+  2. Visual selections can be combined with operators such as:
+	d	delete
+	c	change
+	y	yank (copy)
 
-  2.  :w FILENAME  writes the current Vim file to disk with name FILENAME.
+  3. :vs splits the pane vertically.
+     :sp splits the pane horizontally.
 
-  3.  v  motion  :w FILENAME  saves the Visually selected lines in file
-      FILENAME.
-
-  4.  :r FILENAME  retrieves disk file FILENAME and puts it below the
-      cursor position.
-
-  5.  :r !dir  reads the output of the dir command and puts it below the
-      cursor position.
+  4. :ls shows open buffers.
 
+  5. :Explore opens Zed's project panel.
 
+  6. :term opens Zed's terminal panel.
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 			 Lesson 1.6.1: THE OPEN COMMAND
 
@@ -730,10 +712,6 @@ NOTE:  You can also read the output of an external command.  For example,
      than a lowercase  o.  Try this on the line below.
 
 ---> Open up a line above this by typing O while the cursor is on this line.
-
-
-
-
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 			Lesson 1.6.2: THE APPEND COMMAND
 
@@ -756,7 +734,6 @@ NOTE:  You can also read the output of an external command.  For example,
 
 NOTE:  a, i and A all go to the same Insert mode, the only difference is where
        the characters are inserted.
-
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 		    Lesson 1.6.3: ANOTHER WAY TO REPLACE
 
@@ -779,7 +756,6 @@ NOTE:  a, i and A all go to the same Insert mode, the only difference is where
 
 NOTE:  Replace mode is like Insert mode, but every typed character deletes an
        existing character.
-
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 			Lesson 1.6.4: COPY AND PASTE TEXT
 
@@ -804,171 +780,207 @@ NOTE:  Replace mode is like Insert mode, but every typed character deletes an
   NOTE: You can also use  y  as an operator:  yw  yanks one word,
         yy  yanks the whole line, then  p  puts that line.
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
-			    Lesson 1.6.5: SET OPTION
+			    Lesson 1.6.5: ZED VIM OPTIONS
 
 
-	  ** Set an option so a search or substitute ignores case **
+       ** Use  :set  to change useful options for the current buffer. **
 
-  1. Search for 'ignore' by entering:  /ignore <ENTER>
-     Repeat several times by pressing  n .
+  1. Type:
 
-  2. Set the 'ic' (Ignore case) option by entering:   :set ic
+	:set number <ENTER>
 
-  3. Now search for 'ignore' again by pressing  n
-     Notice that Ignore and IGNORE are now also found.
+     to show line numbers.
 
-  4. Set the 'hlsearch' and 'incsearch' options:  :set hls is
+  2. Type:
 
-  5. Now type the search command again and see what happens:  /ignore <ENTER>
+	:set relativenumber <ENTER>
 
-  6. To disable ignoring case enter:  :set noic
+     to show relative line numbers around the cursor.
 
-NOTE:  To remove the highlighting of matches enter:   :nohlsearch
-NOTE:  If you want to ignore case for just one search command, use  \c
-       in the phrase:  /ignore\c <ENTER>
-~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
-			       Lesson 1.6 SUMMARY
+  3. Type:
+
+	:set norelativenumber <ENTER>
+
+     to turn relative line numbers off again.
+
+  4. Search for 'ignore' using:
 
-  1. Type  o  to open a line BELOW the cursor and start Insert mode.
-     Type  O  to open a line ABOVE the cursor.
+	/ignore <ENTER>
 
-  2. Type  a  to insert text AFTER the cursor.
-     Type  A  to insert text after the end of the line.
+  5. Enable case-insensitive searching with:
 
-  3. The  e  command moves to the end of a word.
+	:set ignorecase <ENTER>
 
-  4. The  y  operator yanks (copies) text,  p  puts (pastes) it.
+     Press  n  several times and notice that IGNORE, Ignore, and ignore can all
+     match.
 
-  5. Typing a capital  R  enters Replace mode until  <ESC>  is pressed.
+  6. Disable it again with:
 
-  6. Typing ":set xxx" sets the option "xxx".  Some options are:
-  	'ic' 'ignorecase'	ignore upper/lower case when searching
-	'is' 'incsearch'	show partial matches for a search phrase
-	'hls' 'hlsearch'	highlight all matching phrases
-     You can either use the long or the short option name.
+	:set noignorecase <ENTER>
 
-  7. Prepend "no" to switch an option off:   :set noic
+---> ignore Ignore IGNORE
 
+NOTE: Zed supports a useful subset of Vim's :set options, integrated with Zed's
+      own editor settings.
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
-		       Lesson 1.7.1: GETTING HELP
+			       Lesson 1.6 SUMMARY
 
+  1. o opens a line BELOW the cursor and enters Insert mode.
+     O opens a line ABOVE the cursor.
 
-		      ** Use the on-line help system **
+  2. a inserts text AFTER the cursor.
+     A inserts text at the end of the line.
 
-  Vim has a comprehensive on-line help system.  To get started, try one of
-  these three:
-	- press the <HELP> key (if you have one)
-	- press the <F1> key (if you have one)
-	- type   :help <ENTER>
+  3. e moves to the end of a word.
 
-  Read the text in the help window to find out how the help works.
-  Type  CTRL-W CTRL-W   to jump from one window to another.
-  Type    :q <ENTER>    to close the help window.
+  4. y yanks (copies) text and p puts (pastes) it.
 
-  You can find help on just about any subject, by giving an argument to the
-  ":help" command.  Try these (don't forget pressing <ENTER>):
+  5. R enters Replace mode until <ESC> is pressed.
 
-	:help w
-	:help c_CTRL-D
-	:help insert-index
-	:help user-manual
+  6. Useful Zed-supported :set commands include:
+	:set number
+	:set nonumber
+	:set relativenumber
+	:set norelativenumber
+	:set ignorecase
+	:set noignorecase
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
-		      Lesson 1.7.2: CREATE A STARTUP SCRIPT
+		       Lesson 1.7.1: THE COMMAND PALETTE
 
 
-			  ** Enable Vim features **
+	 ** In Zed Vim mode,  :  opens Zed's command palette. **
 
-  Vim has many more features than Vi, but most of them are disabled by
-  default.  To start using more features you should create a "vimrc" file.
+  1. Press <ESC> to enter Normal mode.
 
-  1. Start editing the "vimrc" file.  This depends on your system:
-	:e ~/.vimrc		for Unix
-	:e ~/_vimrc		for Windows
+  2. Press  : .
 
-  2. Now read the example "vimrc" file contents:
-	:r $VIMRUNTIME/vimrc_example.vim
+     You can type supported Vim-style Ex commands such as:
 
-  3. Write the file with:
-	:w
+	:w		save
+	:q		close the current buffer
+	:wq		save and close
+	:vs		vertical split
+	:sp		horizontal split
+	:ls		show buffers
+	:Explore	open the project panel
+	:term	open the terminal
 
-  The next time you start Vim it will use syntax highlighting.
-  You can add all your preferred settings to this "vimrc" file.
-  For more information type  :help vimrc-intro
+  3. Press <ESC> to dismiss the palette without running a command.
 
+NOTE: Zed does not emulate every Vim Ex command.  The command palette combines
+      Vim-style aliases with native Zed commands.
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
-			     Lesson 1.7.3: COMPLETION
-
-
-	      ** Command line completion with CTRL-D and <TAB> **
+		      Lesson 1.7.2: ZED-SPECIFIC VIM FEATURES
 
-  1. Make sure Vim is not in compatible mode:  :set nocp
 
-  2. Look what files exist in the directory:  :!ls   or  :!dir
+   ** Zed adds modern editor features while keeping Vim's editing model. **
 
-  3. Type the start of a command:  :e
+  Zed's Vim mode includes functionality that often requires plugins in Vim.
 
-  4. Press  CTRL-D  and Vim will show a list of commands that start with "e".
+  Some useful examples are:
 
-  5. Type  d<TAB>  and Vim will complete the command name to ":edit".
+	ys	surround a text object
+	cs	change surrounding characters
+	ds	delete surrounding characters
 
-  6. Now add a space and the start of an existing file name:  :edit FIL
+	gc	comment or uncomment a Visual selection
+	gcc	comment or uncomment the current line
 
-  7. Press <TAB>.  Vim will complete the name (if it is unique).
+  Zed also provides syntax-aware motions and text objects through its parser.
 
-NOTE:  Completion works for many commands.  Just try pressing CTRL-D and
-       <TAB>.  It is especially useful for  :help .
+  These commands are most useful in source-code files, where Zed knows the
+  language and syntax.
 
+NOTE: Comment commands depend on the current file type, so they may not visibly
+      change this plain-text tutor.
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
-			       Lesson 1.7 SUMMARY
+			     Lesson 1.7.3: NAVIGATING CODE IN ZED
+
 
+   ** Zed connects Vim-style shortcuts to language-server navigation. **
 
-  1. Type  :help  or press <F1> or <HELP>  to open a help window.
+  When editing a supported source-code file, try:
 
-  2. Type  :help cmd  to find help on  cmd .
+	gd	go to definition
+	gD	go to declaration
+	gy	go to type definition
+	gI	go to implementation
 
-  3. Type  CTRL-W CTRL-W  to jump to another window.
+	gs	find a symbol in the current file
+	gS	find a symbol in the project
 
-  4. Type  :q  to close the help window.
+	]d	go to the next diagnostic
+	[d	go to the previous diagnostic
+	gh	show inline error information
+	g.	open code actions
 
-  5. Create a vimrc startup script to keep your preferred settings.
+  These are Zed-specific integrations rather than classic Vim commands.
+  They let you keep a modal editing workflow while using Zed's language-server
+  features.
 
-  6. When typing a  :  command, press CTRL-D to see possible completions.
-     Press <TAB> to use one completion.
+  You do not need to practice these commands inside this plain-text tutor.
+  Return to them when you are editing a real codebase.
+~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
+			       Lesson 1.7 SUMMARY
 
 
+  1. In Normal mode, : opens Zed's command palette.
 
+  2. Zed supports familiar Vim-style commands for saving, closing buffers,
+     splitting panes, showing buffers, and opening panels.
 
+  3. Zed includes built-in surround and commenting commands such as:
+	ys	cs	ds	gc	gcc
 
+  4. Zed integrates Vim-style shortcuts with language-server navigation:
+	gd	gD	gy	gI	gs	gS
 
+  5. Diagnostics can be navigated with:
+	]d	[d
 
+  6. Zed's Vim mode is intentionally not a one-to-one copy of Vim.  It keeps
+     the modal editing model while using Zed's own parser, search system,
+     multiple cursors, language servers, panes, and panels.
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
+  This concludes Chapter 1 of the Zed Vim Tutor.
+
+  You have practiced the core editing model:
+
+	move -> operate -> repeat
+
+  More importantly, you have practiced composing commands instead of treating
+  each Vim key as an isolated shortcut.
+
+  Continue using these commands in real files.  Once they feel natural, useful
+  topics to explore include:
+
+	text objects
+	the . repeat command
+	registers
+	marks
+	macros
+	surround
+	multiple cursors
+	Tree-sitter text objects
+	language-server navigation
+	custom Zed keybindings
 
-  This concludes Chapter 1 of the Vim Tutor.  Consider continuing with
-  Chapter 2 which covers registers, marks and the use of text objects.
+  Run `zedtutor` again whenever you want a fresh practice copy.
 
-  It was intended to give a brief overview of the Vim editor, just enough to
-  allow you to use the editor fairly easily. It is far from complete as Vim
-  has many many more commands.
 
-  Read the user manual next: ":help user-manual".
+  ---------------------------------------------------------------------------
+  UPSTREAM ATTRIBUTION
+  ---------------------------------------------------------------------------
 
-  For further reading and studying, this book is recommended:
-	Vim - Vi Improved - by Steve Oualline
-	Publisher: New Riders
-  The first book completely dedicated to Vim.  Especially useful for beginners.
-  There are many examples and pictures.
-  See https://iccf-holland.org/click5.html
+  This tutorial is derived from the Vim Tutor distributed with Vim.
 
-  This book is older and more about Vi than Vim, but also recommended:
-	Learning the Vi Editor - by Linda Lamb
-	Publisher: O'Reilly & Associates Inc.
-  It is a good book to get to know almost anything you want to do with Vi.
-  The sixth edition also includes information on Vim.
+  The original tutorial was written by Michael C. Pierce and Robert K. Ware,
+  Colorado School of Mines, using ideas supplied by Charles Smith,
+  Colorado State University.
 
-  This tutorial was written by Michael C. Pierce and Robert K. Ware,
-  Colorado School of Mines using ideas supplied by Charles Smith,
-  Colorado State University.  E-mail: bware@mines.colorado.edu.
+  It was modified for Vim by Bram Moolenaar.
 
-  Modified for Vim by Bram Moolenaar.
+  This version has been adapted for use with Zed's Vim mode.
 
+  See the repository's Vim License and attribution files for licensing details.
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
